{
  "name": "city-guesser",
  "version": "1.0.0",
  "description": "A city guessing game using Google Maps",
  "main": "index.js",
  "scripts": {
    "build": "node build.js",
    "start": "npm run build && serve dist"
  },
  "dependencies": {
    "dotenv": "^16.4.1",
    "serve": "^14.2.1"
  }
}
